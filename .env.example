# Copy to .env for local development (never commit .env)

# Kaggle API credentials — only needed to push kernels or download sample data via API
# KAGGLE_USERNAME=your_username
# KAGGLE_KEY=your_api_key

# Override data root for local prototyping (default: data/sample)
# RSNA_DATA_ROOT=data/sample
